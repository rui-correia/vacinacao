micronaut:
  application:
    name: vacinacao
datasources:
  default:
    url: jdbc:postgresql://localhost:5433/vacinasdb
    driverClassName: org.postgresql.Driver
    username: postgres
    password: postgres

jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: update
        show_sql: true
